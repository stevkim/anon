import { Loader } from 'lucide-react';

const ComponentLoader = () => {
	return (
		<>
			<Loader className="animate-spin-slow h-10 w-10 mx-auto mt-4 text-gray-400" />
		</>
	);
};

export default ComponentLoader;
